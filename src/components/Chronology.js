import React, { Component } from 'react';

class Chronology extends Component {

  render() {

    let {chronologyDate} = this.props

    return (
      <React.Fragment>
        <span>{}</span>
      </React.Fragment>
    )

  }
}

export default Chronology;
