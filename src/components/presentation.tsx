import React, { useCallback, useState } from "react";

interface Props {
    manifest: string;
}

export const Presentation: React.FunctionComponent<Props> = () => {
    return (
        <>
        </>
    );
};