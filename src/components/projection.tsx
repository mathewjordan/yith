import React, { useCallback, useState } from "react";

interface Props {
    manifest: string;
}

export const Projection: React.FunctionComponent<Props> = () => {
    return (
        <>
        </>
    );
};